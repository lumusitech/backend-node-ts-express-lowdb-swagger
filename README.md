# Simple CRUD API with Node, Express, LowDB and Swagger (documentation)

After clone, run:

```sh
npm install
```

# documentation:

In browser:

```http
http://localhost:3000/docs
```
## Note: 
test API from requests folder using a vscode's plugin named: "REST Client"
Or use other clients like "Postman"